# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets). Run `pnpm changeset` to record a change intended for release. See `docs/npm-design-system-implementation-plan.md` §10 for the release strategy this project follows.
